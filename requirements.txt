# === Обработка данных ===
pandas==1.5.3          # Для работы с DataFrame и временными рядами
numpy==1.23.5          # Математические операции и работа с массивами

# === Работа с API и сетью ===
requests==2.28.2       # Отправка HTTP-запросов (REST API)
websocket-client==1.6.1  # Подключение к WebSocket API для реального времени

# === Машинное обучение и AI ===
scikit-learn==1.2.2    # Базовые алгоритмы, предобработка, кросс-валидация
xgboost==1.7.3         # Градиентный бустинг (эффективен на табличных данных)
lightgbm==3.3.5        # Быстрый градиентный бустинг, часто даёт хорошие результаты
# Ниже библиотеки для нейросетей (устанавливайте только при необходимости)
torch==2.0.1           # PyTorch для глубокого обучения (например, LSTM для временных рядов)
tensorflow==2.15.0     # TensorFlow (опционально, если предпочтёте его для нейросетей)

# === Технический анализ ===
ta==0.10.2             # Удобная библиотека для расчёта индикаторов (SMA, EMA, RSI, MACD и др.)
TA-Lib==0.6.3          # Более продвинутая библиотека технического анализа (требует установки системных зависимостей)
# Примечание: Можно использовать одну из двух библиотек для индикаторов, чтобы избежать дублирования.

# === Визуализация данных ===
matplotlib==3.7.1      # Стандартная библиотека для построения графиков
seaborn==0.12.2        # Расширение для улучшенной визуализации и статистических графиков

# === Логирование и мониторинг ===
loguru==0.7.0          # Простая и мощная библиотека для логирования событий

# === Управление конфигурацией ===
python-dotenv==1.0.0   # Работа с переменными окружения через .env файлы (хранение API ключей, секретов и настроек)

# === Бэктестинг и симуляция стратегий ===
backtrader==1.9.76.123 # Популярная библиотека для исторического тестирования торговых стратегий

# === Параллельные вычисления и оптимизация ===
joblib==1.3.0          # Используется для параллельной обработки (ускоряет обучение моделей, поиск по гиперпараметрам)
